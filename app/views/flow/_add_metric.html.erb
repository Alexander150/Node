<div class="add-metric-container" id="add_metric_{{e.id}}">
	<!-- {{e}} -->
	<div style="text-align: right; height: auto; width: 100%;">
		<i style="right: 2px; top: 2px;" ng-click="closeAddMetric(e);" class="fal fa-times">X</i>
	</div>
	<div style="text-align: center; height: auto; width: 100%;">
<!-- 		<div ng-repeat="current_metric_value in e.node.metric_values" ng-if="!current_metric_value.has_to_be_entered">
			{{current_metric_value.has_to_be_entered}}
		</div> -->
		<select id="desired-metric-{{e.id}}" class="metric-creation-input" name="metric_name">
			<% Metric.all.each do |metric| %>
				<option ng-if="!current_metric_value.has_to_be_entered" ng-if="current_metric_value.metric.id != metric.id" value="<%= metric.name%>">
					<%= metric.name%>
				</option>
			<% end %>
		</select>
		<input id="desired-metric-value-{{e.id}}" class="metric-creation-input" name="metric_metric_value_value" placeholder="Значение метрики">
		<button ng-click="sendAddNodeData(e);">
			СОЗДАТЬ
		</button>
	</div>
</div>